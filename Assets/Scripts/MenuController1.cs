using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MenuController1 : MonoBehaviour {

	// Use this for initialization

	/*public GameObject guiMenu;
	public float timeMenu = 1;

	private MenuRotation mr;

	private RectTransform[] rtArray;
	private Vector3[] startingScale;
	private float count = 0;

	void Start () {
		mr = gameObject.GetComponent<MenuRotation> ();
	}
	
	// Update is called once per frame
	void Update () {
		if (rtArray != null && count < 1) {
			count += Time.deltaTime / timeMenu;
			for (int i = 0; i < rtArray.Length; i++) {
				rtArray [i].localScale = startingScale[i] * count;
			}
		}
	}

	public void enableMenu(){
		
	}*/
}
